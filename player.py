# Input answer
# Input data, if guesser failed to guess

#
#
# def next_answer(self, b):
#         """ prompts player for the next move and runs that play
#             if it is a legitimate one, otherwise says, try again
#         """
#         while True:
#             selection = int(input('Enter a column: '))
#             if b.can_add_to(selection) == True:
#                 self.num_moves += 1
#                 return selection

#             else:
#                 print('Try again!')